

FUNCTIONs = {
    'mopho': {
        'check': mopho_check_patch,
        'unpack': mopho_unpack
    }
}

def mopho_check_patch(message):
    """Check if this sysex message is a patch for a mopho synth"""
    pass

def mopho_unpack(data):
    """Unpack data into list of parameter values for a mopho patch"""
    pass
